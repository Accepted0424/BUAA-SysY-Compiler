#include <stdio.h>

int getint() {
    int t;
    scanf("%d",&t);
    while(getchar()!='\n');
    return t;
}

void f(int x) {
    return;
}

int g(int x) {
    return x;
}

int main() {
    printf("23373112");

    for (;;) {
        int x = getint();
        if (x == 0) {
            break;
        } else {
            continue;
        }
        printf("Input: %d\n", x);
    }

}